# Development Commands

<!-- init-project auto-fills these placeholders. Manual editing also works. -->

## Dev Environment

| Action | Command |
|--------|---------|
| Start | `bun run dev` |
| Stop | `Ctrl+C` |
| Restart | `bun run dev` |
| Logs | N/A (dev server output in terminal) |

## Quality & Testing

| Action | Command |
|--------|---------|
| Lint (Check) | `bunx ultracite check` |
| Lint (Fix) | `bunx ultracite fix` |
| Type Check | `bunx tsc --noEmit` |
| Test (All) | `bunx vitest run` |
| Test (Watch) | `bunx vitest` |
| Coverage | `bunx vitest run --coverage` |

## Test Users (Dev Seed)

N/A — static portfolio site, no user accounts.
